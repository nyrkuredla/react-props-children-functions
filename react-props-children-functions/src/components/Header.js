import React, { Component } from 'react';


export default class Header extends Component {
  render() {
    return (
      <nav className="jumbotron">Type stuff, make Rex say stuff. Simple as that.</nav>
    );
  }
}
